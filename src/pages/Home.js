import react from 'react'

const Home = (query) =>(
    <form>
        <input type="text" placeholder="User"/>
        <input type="password" placeholder="Password"/>
    </form>
)

export default Home;